{"Key":{"Uuid":"b9a20181-2925-4ade-925e-a2fd05fcbf9b","Effect":"#include <cmath>\nextern \"C\"\nvoid score_rgba(unsigned char* rgba, int width, int height, int t)\n{\n    int k = 0;\n    // Clear screen\n    for(int j = 0; j < height; j++)\n    {\n        for(int i = 0; i < width; i++)\n        {\n            rgba[k++] = 0;\n            rgba[k++] = 0;\n            rgba[k++] = 0;\n            rgba[k++] = 255;\n        }\n    }\n    \n    // Reset k for star drawing\n    // Generate stars using LFSR for pseudo-random positions\n    unsigned int seed = 0x1337;\n    for(int star = 0; star < 256; star++)\n    {\n        // LFSR for deterministic \"random\" positions\n        seed = (seed * 1103515245 + 12345) & 0x7fffffff;\n        int x = (seed % width);\n        seed = (seed * 1103515245 + 12345) & 0x7fffffff;\n        int y = (seed % height);\n        \n        // Parallax layers (3 speeds)\n        int layer = star & 3;\n        int speed = (layer + 1);\n        int z = ((t * speed) + star * 7) & 511;\n        \n        // Project 3D to 2D\n        int qq = z > 256 ? 1 : (256 - z + 1);\n        int px = width/2 + ((x - width/2) * 256) / qq;\n        int py = height/2 + ((y - height/2) * 256) / qq;\n        \n        if(px >= 0 && px < width && py >= 0 && py < height)\n        {\n            int idx = (py * width + px) * 4;\n            int brightness = 255 - (z >> 1);\n            rgba[idx] = brightness;\n            rgba[idx + 1] = brightness;\n            rgba[idx + 2] = brightness;\n        }\n    }\n}"},"Name":"Stars 93","Preset":[]}