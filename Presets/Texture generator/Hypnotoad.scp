{"Key":{"Uuid":"b9a20181-2925-4ade-925e-a2fd05fcbf9b","Effect":"#include <cmath>\nextern \"C\"\nvoid score_rgba(unsigned char* rgba, int width, int height, int t)\n{\n    int k = 0;\n    float cx = width / 2.0;\n    float cy = height / 2.0;\n    for(int j = 0; j < height; j++)\n    {\n        for(int i = 0; i < width; i++)\n        {\n            float dx = i - cx;\n            float dy = j - cy;\n            float dist = sqrt(dx * dx + dy * dy);\n            int ripple = 128 + 127 * sin(dist * 0.1 - t * 0.1);\n            \n            rgba[k++] = ripple;\n            rgba[k++] = ripple * (dist / (width/2));\n            rgba[k++] = 255 - ripple;\n            rgba[k++] = 255;\n        }\n    }\n}"},"Name":"Hypnotoad","Preset":[]}